using System.Collections.Generic;
using Game.Scripts.Game.Character.Skin;
using JetBrains.Annotations;
using UnityEngine;

namespace Game.Scripts.Game.Character.Base
{
    public class CharacterView : MonoBehaviour
    {/*
        // Temporary CanBeNull
        [CanBeNull, SerializeField] private string GroundedParamName;
        [CanBeNull, SerializeField] private string WalkParamName;
        [CanBeNull, SerializeField] private string RunParamName;
        [CanBeNull, SerializeField] private string PunchParamName;
        [CanBeNull, SerializeField] private string ItemPointName;
        [CanBeNull, SerializeField] private CharacterSkin CharacterSkin;
        
        private CharacterSkin _characterSkin;
        private Transform _itemPoint;
        
        public void SetGroundedParam(bool value)
        {
            CharacterSkin.Animator.SetBool(GroundedParamName, value);
        }

        public void SetWalkParam(bool value)
        {
            CharacterSkin.Animator.SetBool(WalkParamName, value);
        }

        public void SetRunParam(bool value)
        {
            CharacterSkin.Animator.SetBool(RunParamName, value);
        }

        public void SetUpWeight(bool value)
        {
            float weight = value ? 1f : 0f;
            CharacterSkin.Animator.SetLayerWeight(1, weight);
        }*/
    }
}